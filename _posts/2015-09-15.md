# Linux 问题汇总
#### samba 添加用户
- 在/etc/samba/smb.conf 中添加
- 重启samba /etc/init.d/samba restart

####  终端连不上Linux
- ps -e | grep ssh检查ssh server是否启动
- sudo apt-get install openssh-server 安装ssh 服务器
- /etc/init.d/ssh start重启服务